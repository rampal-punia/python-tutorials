'''Sorted function example: Sort a list of tuples by alphabets.
'''

list_of_tuples = [(401, 's'), (404, 'v'), (402, 'r'), (403, 'd'), (407, 'r')]

sorted_tuples = sorted(list_of_tuples)

print(sorted_tuples)
